import additionalInfo from '@/project-config/template/personLoan/apply/additionalInfo'
import {mergeInfo} from '@/utils/util'

// 要添加的组件
const modifyPageInfo = {
}
export default mergeInfo(additionalInfo, modifyPageInfo)
